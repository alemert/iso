user-data:
  autoinstall:
    identity:
      hostname: kubi02
    network:
      version: 2
      ethernets:
        all-eth:
          match:
            name: "*"
          dhcp4: true
          dhcp4-overrides:
            send-hostname: true
            hostname: kubi02

meta-data:
  instance-id: iid-kubi02
  local-hostname: kubi02
